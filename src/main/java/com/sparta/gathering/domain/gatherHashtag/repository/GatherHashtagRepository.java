package com.sparta.gathering.domain.gatherHashtag.repository;

import com.sparta.gathering.domain.gatherHashtag.entity.GatherHashtag;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GatherHashtagRepository extends JpaRepository<GatherHashtag, Long> {
}
