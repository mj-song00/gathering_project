services:
  app:
    image: gathering-build:latest
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    networks:
      - app-network
    restart: on-failure

networks:
  app-network:
    driver: bridge
